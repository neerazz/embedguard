# Core dependencies (Python >=3.10)
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
pydantic>=2.0.0
loguru>=0.7.0

# Optional ML paths are declared by the package extras:
#   pip install -e '.[neural]'
#   pip install -e '.[vector]'
# Development tools are declared in pyproject.toml:
#   pip install -e '.[dev]'
